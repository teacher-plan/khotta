-- ════════════════════════════════════════════════════════════════
-- أعمدة بيانات حساب المعلم المولّد، مخزّنة على صف الترويج نفسه
-- لتعمل عملية التوليد/العرض من أي جهاز (بدل الاعتماد على متصفح المشرف).
-- الجدول pre_registrations محمي أصلاً بـ RLS للمشرف فقط، فالوصول لهذه
-- البيانات محصور بالمشرف.
-- ════════════════════════════════════════════════════════════════

alter table public.pre_registrations
  add column if not exists account_email    text,
  add column if not exists account_password text;
