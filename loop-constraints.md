# قيود العمل — منصّة خُطّة (الحلقة الأولى)

> يقرأ هذا الملف skill `loop-constraints` في بداية كل تشغيل، وقواعده **ملزِمة**.
> أضيفي قواعدك في آخر الملف بلغة عادية.

هذه القواعد ليست عامة — كل واحدة منها مكتوبة بعد عطل وقع فعلاً في هذا
المشروع، والسبب مذكور معها كي لا تُحذف بحسن نيّة.

## النشر والدفع

- `main` يُنشر تلقائياً إلى GitHub Pages عبر `.github/workflows/deploy.yml`.
  **كل دفعة إلى `main` تصل المعلمات فوراً.** لا تدفعي إليه إلا بإذن صريح
  في الرسالة الحالية؛ الإذن السابق لا يمتد إلى عمل جديد.
- طوّري دائماً على `claude/wizardly-brahmagupta-zzk93i`.
- بوت `bump-sw-cache.yml` يدفع إلى `main` من تلقائه بعد كل نشر. لذلك:
  **افحصي `origin/main` قبل كل دمج، وأعيدي التأسيس فوقه لا العكس** — وإلا
  أرجعتِ `sw.js` إلى إصدار كاش أقدم فعلّقتِ المعلمات على نسخة قديمة.
- الدمج بـ `--ff-only` فقط. لا commit دمج في تاريخ `main`.
- لا `--force` على `main` إطلاقاً. و`--force-with-lease` على فرع التطوير فقط.

## بيانات المعلمات — الأشد حساسية

- ملف المعلمة (`cycle1_profiles.data`) يحوي صفوفها وطالباتها ونجومها وسجل
  السلوك وجدولها وخطتها. **أي كتابة عليه بحالة غير مؤكّدة تمحوها.**
- عند أي قراءة من Supabase: **افحصي `error` صراحةً**. `supabase-js` لا يرمي
  استثناءً عند فشل الشبكة بل يُرجع `{data:null,error}` — فلا تلتقطه `catch`.
  هذا بعينه ما جعل انقطاعاً عابراً يُظهر معالج الإعداد ثم يمحو بيانات معلمة.
- لا تدخلي التطبيق بملف مجهول الحال. الفشل ← تسجيل خروج ورسالة، لا متابعة.
- `planGet` وأمثاله يجب أن تضمن شكل البيانات دائماً، لا عند الغياب وحده —
  ملف قديم ناقص الحقول كان يُخفي قسم الخطة بالكامل.

## قاعدة البيانات

- **لا تُطبّقي ترحيلاً على قاعدة الإنتاج.** لا يوجد `supabase` CLI هنا،
  وشبكة البيئة تحجب `supabase.co`. اكتبي الترحيل في
  `supabase/migrations/` وسلّمي نصّه للإنسان لينفّذه في اللوحة.
- تحققي من كل ترحيل على PostgreSQL محلي قبل التسليم: أنه يمر، وأنه آمن
  للتكرار، وأن `ON CONFLICT` يطابق فهرساً فريداً قائماً بأعمدته تماماً.
- لا تُضعِفي سياسة RLS قائمة لتسهيل ميزة. `games_own` تعزل ألعاب كل معلمة —
  ثقبها يكشف ما لم تختر مشاركته. انسخي الحمولة بدل الإشارة إليها.
- المفتاح العام (`SUPABASE_ANON_KEY`) في `cycle1.html` بحكم التصميم.
  **مفتاح الخدمة لا يدخل المستودع إطلاقاً.**

## الواجهة

- الهوية ثلاثة ألوان: القشدي والأحمر والزيتوني. لا تستحدثي لوناً رابعاً؛
  عالجي التباين بتغميق نص أو إنهاء تدرّج عند لون قائم.
- كل نص يجب أن يجتاز WCAG AA (٤٫٥:١، أو ٣:١ للكبير). فحصٌ جاهز في
  `test-contrast.mjs` — شغّليه بعد أي تغيير لوني.
- أهداف اللمس ≥ ٢٤×٢٤px. ولا تُخفي فعلاً خلف `:hover` وحده — أجهزة اللمس
  لا تعرفه، وزر حذف المهمة ظلّ متعذّراً على الجوال بسببه.
- طبقات z-index: `#wk-sheet` = 940، `.c1-modal` = 941. أي نافذة تُفتح فوق
  الجدول الكامل يجب أن تتجاوز 940.
- لا تمرير أفقي على أي عرض من ٣٢٠px فأعلى.

## الكود والاختبار

- شغّلي الاختبارات قبل أي دفع: `m` `t` `v` `test-edge` `test-cache`
  `test-contrast` `pool` `variants` `gen` — وتحتاج خادماً محلياً على 8899.
- **الاختبارات تعيش في مجلد مؤقّت يُمسح مع الحاوية.** إن احتجتِها باقيةً
  فأودِعيها المستودع أولاً.
- لا تُعطّلي اختباراً لتُخضِر النتيجة.
- إصلاح واحد لكل تشغيل. لا إعادة هيكلة عابرة مع إصلاح عطل.
- بعد ثلاث محاولات فاشلة على البند نفسه: توقّفي وأبلغي، لا تُكرّري.
- `cycle1.html` ملف واحد ٤٦٦KB يحوي التطبيق كله. تحققي من صحة الصياغة بعد
  كل تعديل (`new Function` على كل كتلة `<script>`).

## المسارات الممنوعة

- `.env`, `.env.*`, `secrets/`, `credentials/`, `auth/`
- `.github/workflows/` — لا تُعدّلي النشر دون إذن صريح
- `sw.js` — يديره البوت؛ تعديله يدوياً يصطدم بترقيته
- `vendor/` — مكتبات خارجية مثبّتة

## التواصل

- أخبري قبل الفعل لا بعده، خصوصاً ما يمسّ `main` أو بيانات المعلمات.
- بلّغي النتائج كما هي: الاختبار الفاشل يُذكر بمخرجاته، والخطوة المتروكة
  تُذكر صراحةً.
- لا تُغلقي مسألة أو PR دون إذن.

## الميزانية

- عند بلوغ ٨٠٪ من السقف اليومي: وضع التقرير فقط، بلا وكلاء فرعيين.
- عند `loop-pause-all`: خروج فوري.

---
<!-- أضيفي قواعدك هنا بلغة عادية — تُقرأ كما هي -->
