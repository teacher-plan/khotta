-- ════════════════════════════════════════════════════════════════════
-- P1(ج) — إجبار PostgREST على إعادة قراءة صلاحيات الجدول.
--
-- الاختبار الحيّ بعد 20260816_p1b_pre_registrations_grants.sql: GRANT
-- نُفِّذ (201) لكن anon ظلّ يُرفَض بـ 42501 نفسها. PostgREST يخزّن
-- الصلاحيات والمخطّط في ذاكرته عند الإقلاع، ولا يُعيد قراءتها تلقائياً
-- عند كل DDL — يحتاج إشعاراً صريحاً.
-- ════════════════════════════════════════════════════════════════════

NOTIFY pgrst, 'reload schema';
